PORT=3000
DB_NAME=passwords.db
DB_PATH=./src/db/passwords.db
# Generate with: openssl rand -hex 32
SECRET_KEY=
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=
# Optional: only required to read legacy AES-CBC records. Generate with: openssl rand -hex 16
ENCRYPTION_IV=
FRONTEND_ORIGIN=http://localhost:5173
